{
  "name": "privseclaw-info",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/build-feeds.mjs && cp feeds.config.json public/ && npx serve public",
    "build": "npx playwright install chromium && node scripts/build-feeds.mjs && cp feeds.config.json public/"
  },
  "dependencies": {
    "patchright": "^1.58.2",
    "playwright": "^1.58.2",
    "rss-parser": "^3.13.0"
  }
}
